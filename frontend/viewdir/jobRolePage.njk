{% extends "template.njk" %}

{% block content %}
    <div class="container">
        <h3 id="roleName" style="color:#575756">{{specification.roleName}}</h3>

        <div class="row">
            <h6>
                <div class="col" id="roleDatePosted">Date Posted: {{specification.datePosted}}</div>
                <div class="col" id="roleHours">{{specification.hours}}</div>
                <div class="col" id="roleLocation">{{specification.location}}</div>
            </h6>
        </div>

        <div class="divider"></div>

        <h5><span style="color:#41679f">Band Level: </span><span id="bandName" style="color:#575756">{{band.bandName}}</span></h5>

        <div class="description">
            <div class="divider"></div>
            <div class="section">
                <h5><span style="color:#41679f">Role Description </span></h5>
                <p id="description">{{specification.roleDesc}}</p>
                {% if specification.link and specification.link !== "" %}
                    <a href="{{specification.link}}"
                        id="specificationLink">View on Sharepoint</a>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}